.input-example {
  padding-bottom: constant(safe-area-inset-bottom);
  /* 兼容 iOS < 11.2 */
  padding-bottom: env(safe-area-inset-bottom);
  /* 兼容 iOS >= 11.2 */
}
.input-example .cell {
  margin-bottom: 32rpx;
}
.input-example .verify {
  font-size: 32rpx;
  color: #0052d9;
}
.input-example .error-msg {
  text-align: right !important;
}
.line {
  display: flex;
  width: 100%;
  height: 3rpx;
  background: lightgray;
  margin-left: 0rpx;
}
.demo-section__wrapper {
  margin: 32rpx 0;
}
.box {
  margin: 0 32rpx;
  margin-top: 20rpx;
}